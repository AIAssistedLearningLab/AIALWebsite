name: Jekyll Site Building
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: [ "self-hosted" ]
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3
      - name: Setup ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          ruby-version: '3.1.4'
      - name: Install dependencies
        run: bundle install
      - name: Setup build branch
        run: git worktree add --orphan -b gh-built ${{ runner.temp }}/${{ github.sha }}_site
      - name: Build site for github
        run: bundle exec jekyll build --config _config.yml -d {{ runner.temp }}/${{ github.sha }}_site
      - name: Switch to the build folder
        run: cd {{ runner.temp }}/${{ github.sha }}_site
      - name: Add all built files
        run: git add -A
      - name: Commit newly built files
        run: git commit -m "Update site"
      - name: Push the new changes
        run: git push

